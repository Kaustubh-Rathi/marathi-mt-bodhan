# secrets
.env
*.env

# credential files — live in ~/.kaggle, HF CLI, or private Kaggle Datasets;
# never in the repo
kaggle.json
access_token*
rclone.conf
sa.json
hf_token.txt
token.txt

# python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ipynb_checkpoints/

# artifacts / weights
*.safetensors
*.bin
*.pt
*.ckpt
*.pth
models/*
!models/.gitkeep
artifacts/
outputs/
runs/
checkpoint_mirror/
wandb/
mlruns/

# data
data/raw/*
data/processed/*
!data/raw/.gitkeep
!data/processed/.gitkeep

# vendor clones (no-license references, local only)
vendor/*

# kaggle / hf caches
.kaggle/
.cache/

# machine-specific helper (rclone binary path); sync_drive.ps1 falls back to PATH
scripts/rclone_path.txt
scripts/kaggle/kernel-metadata.json
